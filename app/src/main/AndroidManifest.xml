<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MovieBuddy"
        tools:targetApi="31">
        <activity
            android:name=".EditCinema"
            android:exported="false" />
        <activity
            android:name=".EditMovie"
            android:exported="false" />
        <activity
            android:name=".CinemaDetails"
            android:exported="false" />
        <activity
            android:name=".CreateCinema"
            android:exported="false" />
        <activity
            android:name=".MovieDetails"
            android:exported="false" />
        <activity
            android:name=".CreateMovie"
            android:exported="false" />
        <activity
            android:name=".MoviePage"
            android:exported="false"
            android:label="@string/title_activity_movie_page"
            android:theme="@style/Theme.MovieBuddy.NoActionBar" />
        <activity
            android:name=".CinemaPage"
            android:exported="false"
            android:label="@string/title_activity_cinema_page"
            android:theme="@style/Theme.MovieBuddy.NoActionBar" />
        <activity
            android:name=".Homepage"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MovieBuddy.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>